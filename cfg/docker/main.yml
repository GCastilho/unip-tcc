version: '3'

services:
  main:
    image: gcastilho/main-server:latest
    ports:
     - 80:80
     - 8085:8085
    restart: unless-stopped
    environment:
      PORT: 80
      MONGODB_URL: ${MONGODB_URL}
